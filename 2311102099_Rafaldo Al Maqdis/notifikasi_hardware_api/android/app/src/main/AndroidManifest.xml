<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  Konfigurasi permission dan komponen Android untuk project ini.
  File ini wajib dikonfigurasi agar kamera, galeri, dan notifikasi berfungsi.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ══════════════════════════════════════════════════════════
         PERMISSION (Izin Akses)
         Semua permission di bawah wajib ada agar fitur berjalan.
         ══════════════════════════════════════════════════════════ -->

    <!-- Izin mengakses kamera perangkat -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Izin membaca media foto (Android 13+ / API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Izin membaca storage (Android 12 ke bawah / API < 33) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Izin menulis ke storage (Android 9 ke bawah, opsional) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Izin menampilkan notifikasi (Android 13+ / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Izin mengakses internet (opsional, untuk fitur masa depan) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Fitur hardware yang dibutuhkan (kamera) -->
    <!-- android:required="false" agar tetap bisa install di device tanpa kamera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- ══════════════════════════════════════════════════════════
         APPLICATION
         ══════════════════════════════════════════════════════════ -->
    <application
        android:label="Notif Hardware API"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <!-- ─ Activity Utama Flutter ─ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- WAJIB ADA untuk v2 embedding -->
            <meta-data
                android:name="flutterEmbedding"
                android:value="2" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ─ FileProvider: Wajib untuk image_picker di Android ─ -->
        <!-- Digunakan agar URI file gambar bisa di-share antar proses -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Flutter deeplinking metadata -->
        <meta-data
            android:name="flutter_deeplinking_enabled"
            android:value="true" />

    </application>

</manifest>
